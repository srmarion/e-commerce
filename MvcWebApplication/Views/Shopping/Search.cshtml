@model MvcWebApplication.ViewModels.Shopping.SearchViewModel

@{
	ViewData["Title"] = "Index";
}

<h4>Shop For Something</h4>

@{
	if (!string.IsNullOrEmpty(Model.Message))
	{
		<div class="text-danger">@Model.Message</div>
	}
}

@* <p>
	<a asp-action="New">Create New</a>
</p> *@
<form asp-action="Search" method="post">
	<div class="form-group">
		<div class="divInline">
			<label asp-for="ShoppingListingSearch.Category" class="control-label"></label>
			<select asp-for="ShoppingListingSearch.Category" asp-items="Model.CategoryList" class="form-select inputInline">
				<option value=""></option>
			</select>
			<button type="submit">Search</button>
		</div>
	</div>
</form>
@if (Model.MenuListingList.Count > 0)
{
	<table class="table">
		<thead>
			<tr>
				<th>Item Id</th>
				<th>Name</th>
				<th>Category</th>
				<th>Cost</th>
				<th></th>
			</tr>
		</thead>
		<tbody>
			@foreach (var item in Model.MenuListingList)
			{
				<tr>
					<td>
						@item.ItemId
					</td>
					<td>
						@item.Name
					</td>
					<td>
						@item.Category
					</td>
					<td>
						@item.Cost.ToString("C2")
					</td>
					<td>
						<!-- asp-route-* attribute defines a GET parameter when calling a controller endpoint -->
				
						<a asp-action="AddToCart" asp-controller="Shopping" asp-route-itemId="@item.ItemId" asp-route-category="@Model.ShoppingListingSearch.Category">Add to Cart</a>
					</td>
				</tr>
			}
		</tbody>
	</table>
}
else
{
	<table class="displayList">
		<tr>
			<td style="width:100%;">
				There are no items in that category.
			</td>
		</tr>
	</table>
}
